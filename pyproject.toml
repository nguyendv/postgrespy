[tool.black]
line-length = 88
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | \.vscode
    | _build
    | buck-out
    | build
    | dist
  )/
  | thisisaexample-4343h.py           # also separately exclude a file named foo.py in
                     # the root of the project
)
'''

